[package]
name = "spki"
version = "0.8.0-rc.1"
edition = "2024"

[dependencies]
real_spki = { package = "spki", version = "=0.8.0-rc.1", default-features = false, features = [] }

[features]
default = []
alloc = ["real_spki/alloc"]
pem = ["real_spki/pem"]
std = ["alloc", "real_spki/std"]
